import { SectionContainer } from "@/components/ui/SectionContainer";

export function DevOpsCloud() {
  return (
    <SectionContainer id="devops">
      <h2 className="text-3xl font-semibold tracking-tight sm:text-4xl">DevOps &amp; Cloud</h2>
      <p className="mt-6 max-w-2xl text-muted">Placeholder — content to be written.</p>
    </SectionContainer>
  );
}
